function calcArPe(altura,largura){

    let areaM = altura * largura;
    let perM = 2 * (altura+largura);
    console.log(`Área da sala: ${area} metros quadrados`);
    console.log(`Perímetro da sala: ${perimetro} metros`);
}

let altura = 5;
let largura = 2;

calcArPe(altura,largura);